<h1>Editing product</h1>

<%= form_for(@product, :url => useradmin_product_path(@product)) do |f| %>
<%= render :partial => 'form', :locals => { :f => f} %>
<% end %>

<%= link_to 'Show', useradmin_product_path(@product) %> |
<%= link_to 'Back', useradmin_products_path %>